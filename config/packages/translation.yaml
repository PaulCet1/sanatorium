framework:
    default_locale: pl
    translator:
        default_path: '%kernel.project_dir%/translations'
        fallbacks:
            - pl
        paths:
            - '%kernel.project_dir%/src/Room/translations'
            - '%kernel.project_dir%/src/Reservation/translations'
            - '%kernel.project_dir%/src/TreatmentProfile/translations'

#        providers:
#            crowdin:
#                dsn: '%env(CROWDIN_DSN)%'
#            loco:
#                dsn: '%env(LOCO_DSN)%'
#            lokalise:
#                dsn: '%env(LOKALISE_DSN)%'
