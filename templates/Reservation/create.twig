{% extends "base.html.twig" %}
{% block body %}
    {% embed 'Structure/Form/create_structure.html.twig' with {'title': data } %}
        {% block form_content %}
            {{ form_start(form) }}
            <div class="form-group">
                {{ form_label(form.client) }}
                {{ form_widget(form.client) }}
            </div>
            <div class="form-group">
                {{ form_label(form.pesel) }}
                {{ form_widget(form.pesel) }}
                {% if form.pesel.vars.errors|length > 0 %}
                    <div class="alert alert-danger">
                        {{ form_errors(form.pesel) }}
                    </div>
                {% endif %}
            </div>
            <div class="form-group">
                {{ form_label(form.referralNumber) }}
                {{ form_widget(form.referralNumber) }}
            </div>

            <div class="form-group">
                {{ form_label(form.plannedStay) }}
                {{ form_widget(form.plannedStay) }}
            </div>
            <div class="form-group">
                {{ form_label(form.numOfPeople) }}
                {{ form_widget(form.numOfPeople) }}
            </div>
            <div class="form-group">
                {{ form_label(form.room) }}
                {{ form_widget(form.room) }}
            </div>

            {{ form_widget(form.save) }}
            </div>
            {{ form_end(form) }}
        {% endblock %}
    {% endembed %}
{% endblock %}